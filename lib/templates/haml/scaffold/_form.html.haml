= form_for @<%= singular_name %> do |f|
  = f.inputs do
  <%- attributes.each do |attribute| -%>
    %tr
      %td=f.label :<%= attribute.name %>
      %td=f.text_field :<%= attribute.name %>
  <%- end -%>

