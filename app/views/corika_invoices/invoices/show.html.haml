.block
  .secondary-navigation
    = render :partial=>'nav'
  .content
    .inner
      %h3
        #{t("invoice",:count=>1)}
        &nbsp;
        #{@invoice.number} vom #{@invoice.invoice_date}
      .container-fluid
        - @invoice.items.each do |i|
          .row
            .col-md-3.text-end
              %label=i.label
            .col-md-1.text-end=i.count
            .col-md-1 x
            .col-md-2.text-end=format_currency(i.price)
            .col-md-2.text-end=format_currency(i.count*i.price)
        %hr.calc
        .row
          .col-md-3.text-end
            %label=t("common.sum")
          .col-md-4.text-end
          .col-md-2.text-end=format_currency(@invoice.sum)
        .row
          .col-md-3.text-end
            %label=t("common.incl_tax",:tax_rate=>@invoice.taxrate)
          .col-md-4.text-end
          .col-md-2.text-end=format_currency(@invoice.sum-@invoice.net_sum)
        %hr.calc_final

