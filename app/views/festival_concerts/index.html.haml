.block
  .secondary-navigation
    =render :partial=>'nav'
  .content
    %h2.title
      = t("common.all")
      = t("festival_concert.p")
    .inner
      %table.table.table-hover.table-condensed
        %tr
          %th=t("common.number")
          %th=t("festival_concert.title")
          %th=t("festival_concert.location")
          %th=t("festival_concert.event_time")
          %th=t("festival_concert.outdoor")
          %th

        = render @festival_concerts
- content_for :sidebar, render(:partial => 'sidebar')


