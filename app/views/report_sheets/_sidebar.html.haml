=render :partial=>'orchestras/search'
.block
  - if not @has_current_report_sheet then
    %h3 Automatisierung
    %ul.navigation
      %li
        =link_to "Datenübernahme as Vorjahr", copy_from_last_year_orchestra_report_sheets_path( @orchestra ) 
