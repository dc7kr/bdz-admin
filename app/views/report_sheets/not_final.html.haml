.block
  .secondary-navigation
    =render :partial=>'nav'
  .content
    %h2.title #{t('report_sheet.not_final')} (gesamt: #{@not_final.size.to_s})
    .inner
      %table.table
        %tr
          %th= t('member.mglnr')
          %th= t('orchestra.orchName')
        -@not_final.each do |r|
          %tr
            %td=r.id

