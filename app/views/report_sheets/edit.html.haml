%h1 Meldebogen bearbeiten
= link_back orchestra_report_sheets_path(@orchestra), t("common.back")

= render 'form'

= link_to 'Show', orchestra_report_sheet_path(@orchestra,@report_sheet)
\|
= link_to 'Back', report_sheets_path
