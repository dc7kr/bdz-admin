.secondary-navigation
  = render :partial=>'nav'
.container
  = entity_row(@tariff, :tariff_type)
  = entity_row(@tariff, :description)
  = entity_row(@tariff, :tag)
  = entity_row(@tariff, :amount, :currency)

