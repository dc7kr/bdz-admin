.block
  .secondary-navigation
    =render :partial=>'nav'
  .content
    %h2.title
      = t("common.edit")
      = t("magazine_sampling.title_s")
    .inner
      = form_for @advertiser do |f|
        = render :partial => "form", :locals => {:f => f}
