.block
  .secondary-navigation
    =render :partial=>'nav'
  .content
    %h2.title
      = t("common.all")
      = t("advertiser.title_p")
    .inner
      %table
        %thead
          %th.first=t('contact.company')
          %th=t('contact.email')
          %th.last
        = render @advertisers

