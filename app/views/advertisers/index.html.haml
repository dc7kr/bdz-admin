.block
  .secondary-navigation
    =render :partial=>'nav'
  .content
    %h2.title
      = t("common.all")
      = t("advertiser.p")
    .inner
      %table.table.table-hover.table-condensed
        %thead
          %th.first=t('contact.company')
          %th=t('contact.email')
          %th.last
        = render @advertisers

