.block
  .secondary-navigation
    =render :partial => 'nav'
  .content
    %h2.title
      = t("web-app-theme.edit", :default => "Edit")
      Einzelmitglied
    .inner
      = form_for (@person_member) do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
