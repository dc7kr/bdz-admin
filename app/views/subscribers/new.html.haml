.block
  .secondary-navigation
    = render :partial =>'nav'
  .content
    %h2.title
      = t("common.new")
      = t("subscriber.s")
    .inner
      = form_for @subscriber do |f|
        = render :partial => "form", :locals => {:f => f}

