%ul
  %li{ :class => 'first'}= link_to_up_path t("common.up"), orchestra_path(@orchestra)
  = nav_to_list OrchestraMember, orchestra_orchestra_members_path(@orchestra)
  = nav_to_new OrchestraMember, new_orchestra_orchestra_member_path(@orchestra)

