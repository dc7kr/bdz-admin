$('#member_account_bookings').html('#{escape_javascript render(@bookings)}');
$('#paginator').html('#{escape_javascript(paginate(@bookings, :remote => true).to_s)}');
$('#entry_info').html('#{escape_javascript(page_entries_info(@bookings).to_s)}');
