.block
  .secondary-navigation
    =render :partial=>'nav'
  .content
    %h2.title
      = t("common.all")
      = t("contact_person.p")
    .inner
      %table.table
        %thead
          %th=label :contact_person, :salutation
          %th=label :contact_person, :first_name
          %th=label :contact_person, :last_name
          %th=label :contact_person, :email
          %th=label :contact_person, :phone
          %th
        = render @contact_people





