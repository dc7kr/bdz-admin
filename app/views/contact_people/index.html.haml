.secondary-navigation
  =render :partial=>'nav'
.container
  %h2.title
    = t("common.all")
    = t("contact_person",:count=>3)
  %table.table.table-sm.table-hover.table-condensed
    %thead
      %th=attr_heading ContactPerson, :salutation
      %th=attr_heading ContactPerson, :first_name
      %th=attr_heading ContactPerson, :last_name
      %th=attr_heading ContactPerson, :email
      %th=attr_heading ContactPerson, :phone
      %th
    %tbody#contact_people
    = render @contact_people





