.block
  .secondary-navigation
    =render :partial=>'nav'
  .content
    %h2.title
      = t("common.all")
      = t("distinction.p")
      = @orchestra.orchName
    .inner
      %table.table.table-hover.table-condensed
        %thead
          %th.first= sortable "dist_date", attr_heading(Distinction, :dist_date)
          %th=attr_heading Distinction, :silver_needles
          %th=attr_heading Distinction, :gold_needles
          %th=attr_heading Distinction, :national_needles
          %th=attr_heading Distinction, :certificates
          %th=attr_heading Distinction, :honorletters
          %th=attr_heading Distinction, :medals
          %th.last &nbsp;
        %tbody#distinctions
          =render @distinctions
      #paginator
        = paginate @distinctions, :remote=>true
      .actions-bar.wat-cf
        .actions
- content_for :sidebar, render(:partial => 'sidebar')
