.block
  .secondary-navigation
    %ul.wat-cf
      = render :partial => 'nav'
  .content
    %h2.title
      = t("web-app-theme.edit", :default => "Edit")
      = t("distinction.s")
    .inner
      %h3= @orchestra.orchName
      = form_for [@orchestra,@distinction], :url => orchestra_distinction_path(@orchestra,@distinction), :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
