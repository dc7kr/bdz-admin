.block
  = f.error_messages :message => "", :header_tag => :h3
.container-fluid
  = form_my_field(f,@distinction, :dist_date, :date)
  = form_my_field(f,@distinction, :gold_needles,:number)
  = form_my_field(f,@distinction, :silver_needles,:number)
  = form_my_field(f,@distinction, :national_needles,:number)
  = form_my_field(f,@distinction, :certificates,:number)
  = form_my_field(f,@distinction, :honorletters,:number)
  = form_my_field(f,@distinction, :medals,:number)
  = form_my_field(f,@distinction, :porto,:currency)
  .actions
    =submit_button
    =cancel_button
