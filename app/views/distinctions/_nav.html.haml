%ul.nav.nav-tabs
  %li.nav-item= link_to_up_path t("common.up"), orchestra_path(params[:orchestra_id])
  = nav_to_list Distinction, orchestra_distinctions_path(@orchestra)
  = nav_to_show @distinction
  = nav_to_edit @distinction
  = nav_to_new Distinction, new_orchestra_distinction_path(@orchestra)
- content_for :entity_menu, render(:partial => 'orchestras/entity_menu', locals: { entity: @orchestra })
