$('#member_events').html('#{escape_javascript render(@member_events)}');
$('#paginator').html('#{escape_javascript(paginate(@member_events, :remote => true).to_s)}');
//$('#entry_info').html('#{escape_javascript(page_entries_info(@member_events).to_s)}');
