.secondary-navigation
  = render :partial =>'nav'
.container
  %h2.title
    = t("common.new")
    = t("member_event",:count=>1)
    - if @member_entity
      = @member_entity.member.mglnr
  .inner
    = render 'form'
