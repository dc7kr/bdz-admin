.block
  .secondary-navigation
    = render :partial=>'nav'
  .content
    .inner
      %h2.title
        =t("magazine_sampling.s")
        =@magazine_sampling.company
      %table
        = render @magazine_sampling.contact,:partial=>"contact"
        %tr
          %td.label #{t("magazine_sampling.count")}:
          %td= @magazine_sampling.count
      .wat-cf
        = edit_button(edit_magazine_sampling_path(@magazine_sampling),@magazine_sampling)
        = del_button(magazine_sampling_path(@magazine_sampling),@magazine_sampling)

- content_for :sidebar, render(:partial => 'sidebar.show')
