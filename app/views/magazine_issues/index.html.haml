.block
  .secondary-navigation
    =render :partial=>'nav'
  .content
    %h2.title
      = t("common.all")
      = t("magazine_issue.p")
    .inner
      %table.table
        %thead
          %th.first=t('common.year')
          %th=t('magazine_issue.nummer')
          %th.last
        = render @magazine_issues
