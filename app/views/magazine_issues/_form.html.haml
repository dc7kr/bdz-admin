= form_for @magazine_issue,:html=>"form-horizontal" do |f|
  =form_my_field(f,@magazine_issue, :year,:number)
  =form_my_field(f,@magazine_issue, :number,:number)
  .actions
    = submit_button
    = cancel_button
