$('#board_contacts').html('#{escape_javascript render(@board_contacts)}');
$('#paginator').html('#{escape_javascript(paginate(@board_contacts, :remote => true).to_s)}');
$('#entry_info').html('#{escape_javascript(page_entries_info(@board_contacts).to_s)}');
