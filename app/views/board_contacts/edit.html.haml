.block
  .secondary-navigation
    = render :partial => 'nav'
  .content
    %h2.title
      = t("common.edit")
      = t('board_contact.s')
    .inner
      = form_for @board_contact do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
