%tr
  %td= board_contact.fullname
  %td
    -if board_contact.contact.nil? then
      INCONSISTENT
    -else 
      = mail_to board_contact.contact.email,board_contact.contact.email
  %td
    = link_to_show board_contact
    = link_to_edit board_contact
    = link_to_delete board_contact

