.block
  .secondary-navigation
    = render :partial =>'nav'
  .content
    %h2.title
      = t("common.new")
      = t("board_contact")
    .inner
      = form_for @board_contact do |f|
        = render :partial => "form", :locals => {:f => f}

