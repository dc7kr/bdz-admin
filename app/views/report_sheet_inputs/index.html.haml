.block
  .secondary-navigation
    =render :partial=>'nav'
  .content
    %h2.title
      = t("common.all")
      = t("report_sheet_input.p")
    .inner
      %table.table
        %thead
          %th=t("common.year")
          %th=t("orchestra.orch_name") 
          %th=t("common.updated_at") 
          %th.last &nbsp;
        %tbody#orchestras
          =render @report_sheet_inputs
      .actions-bar.wat-cf
        .actions

