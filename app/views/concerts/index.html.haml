.block
  .secondary-navigation
    =render :partial=>'nav'
  .content
    %h2.title
      = t("common.all")
      Konzerte
    .inner
      %table.table.table-hover.table-condensed
        %tr
          %th.first 
            = sortable "datum", t("concert.datum")
          %th
            = t("concert.zeit")
          %th
            = t("concert.interpret")
          %th
            = sortable "ort", t("concert.ort")
          %th
            = t("concert.url")
          %th.last &nbsp;
        %tbody#concerts
          =render @concerts
      #paginator
        = paginate @concerts, :remote=>true
      .actions-bar.wat-cf
        .actions
