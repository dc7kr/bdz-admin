.block
  .secondary-navigation
    = render :partial => "nav"
  .content
    %h2.title
      = t("web-app-theme.edit", :default => "Edit")
      Konzert
    .inner
      = form_for @concert, :url => concert_path(@concert), :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
