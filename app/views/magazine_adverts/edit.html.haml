.block
  .secondary-navigation
    =render :partial=>'nav'
  .content
    %h2.title
      = t("common.edit")
      = t("magazine_advert.title_s")
    .inner
      = form_for @magazin_advert do |f|
        = render :partial => "form", :locals => {:f => f}
