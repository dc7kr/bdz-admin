.block
  .secondary-navigation
    =render :partial=>'nav'
  .content
    %h2.title
      = t("common.all")
      = t("magazine_advert.p")
    .inner
      %table.table
        %thead
          %th.first=t('common.year')
          %th=t('magazine_advert.number')
          %th.last
        = render @magazine_adverts
