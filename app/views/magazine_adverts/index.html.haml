.block
  .secondary-navigation
    =render :partial=>'nav'
  .content
    %h2.title
      = t("common.all")
      = t("magazine_advert.p")
    .inner
      %table.table.table-hover.table-condensed
        %thead
          %th.first=t('advertiser.s')
          %th=t('magazine_issue.number')
          %th=t('magazine_issue.year')
          %th=t('magazine_advert.advert_type')
          %th.last
        = render @magazine_adverts
