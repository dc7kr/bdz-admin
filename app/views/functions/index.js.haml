$('#functions').html('#{escape_javascript render(@functions)}');
$('#paginator').html('#{escape_javascript(paginate(@functions, :remote => true).to_s)}');
$('#entry_info').html('#{escape_javascript(page_entries_info(@functions).to_s)}');
