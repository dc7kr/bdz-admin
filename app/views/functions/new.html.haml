.block
  .secondary-navigation
    = render :partial =>'nav'
  .content
    %h2.title
      = t("common.new")
      = t("function")
    .inner
      = form_for @function do |f|
        = render :partial => "form", :locals => {:f => f}

