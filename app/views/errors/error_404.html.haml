.block
  .secondary-navigation
  .content
    %h2.title
      = t("common.error_404")
    .inner
      %p= @exception 
      -if current_user != nil and current_user.admin?
        %pre
          =@exception.backtrace.join("\n")
