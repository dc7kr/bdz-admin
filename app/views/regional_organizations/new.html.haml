.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", regional_organizations_path
      %li.active= link_to "#{t("web-app-theme.new", :default => "New")}", new_regional_organization_path
  .content
    %h2.title
      = t("web-app-theme.new", :default => "New")
      RegionalOrganization
    .inner
      = form_for :regional_organization, :url => regional_organizations_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
