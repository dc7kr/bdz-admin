Hallo #{@appl.contact_person.first_name} #{@appl.contact_person.last_name},

die Änderung Ihrer Daten für das BDZ eurofestival zupfmusik #{@appl.year} wurde gespeichert. 

- if not @appl.tickets.nil? and @appl.tickets > 0 
  Bestellte Festival-Tickets: 
  regulär: #{@appl.tickets}
  ermäßigt: #{@appl.tickets_red}
  Solisten-Tageskarten: #{@appl.soloist_tickets}
  

Sie können diese jederzeit unter 
= ef_festival_application_url(@appl)
einsehen und bearbeiten.

Diese Mail wurde automatisch erstellt. Bitte nicht antworten.

