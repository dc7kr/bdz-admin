-if page.current?
  %li.page-item.active
    %span.page-link
      =page
      %span.sr-only (current)
-else
  %li.page-item= link_to page, url, {:class=> "page-link", data: { "turbo-prefetch": false }, :remote => remote, :rel => page.next? ? 'next' : page.prev? ? 'prev' : nil }
