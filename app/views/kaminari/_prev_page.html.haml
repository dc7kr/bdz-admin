%li.page-item
  - if not current_page.first?
    = link_to url, { :rel => 'prev', :data=> { :remote => remote},:class=>"page-link"} do
      %span{:aria-hidden=>"true"} &lt;
      %span.sr-only Previous
