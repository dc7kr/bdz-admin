.block
  .secondary-navigation
    =render :partial=>'nav'
  .content
    %h2.title
      = t("common.all")
      = t('user.p')
    .inner
      %table.table
        %thead
          %th.first= t("user.username")
          %th.first= t("user.email")
          %th.first= t("user.last_sign_in_at")
          %th.first= t("user.last_sign_in_ip")
          %th.first= t("user.sign_in_count")
          %th.last &nbsp;
        %tbody#users
          =render @users
      #paginator
        = paginate @users, :remote=>true
      .actions-bar.wat-cf
        .actions
- content_for :sidebar, render(:partial => 'sidebar')
