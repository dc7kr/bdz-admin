.block
  .secondary-navigation
    =render :partial=>'nav'
  .content
    %h2.title
      = t("common.all")
      = t("event_meal.title")
    .inner
      %table.table
        %thead
          %tr
            %th=t('event_meal.tln')
            %th=t('event_meal.veg')
            %th=t('event_meal.name')
            %th=t('event_meal.email')
            %th=t('event_meal.orderdate')
            %th &nbsp;
        = render @event_meals
