.block
  .secondary-navigation
    = render :partial => 'nav'
  .content
    %h2.title
      = t("common.all")
      = t('feature_request.p')
    .inner
      %table.table.table-hover.table-condensed
        %tr
          %th=t('feature_request.priority')
          %th=t('feature_request.status.title')
          %th=t('feature_request.title')
          %th
        = render @feature_requests

