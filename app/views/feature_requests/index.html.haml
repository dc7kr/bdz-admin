.block
  .secondary-navigation
    = render :partial => 'nav'
  .content
    %h2.title
      = t("common.all")
      = t('feature_request',:count=>3)
    .inner
      %table.table-sm.table-hover.table-condensed
        %tr
          %th=attr_heading(FeatureRequest, :priority)
          %th=attr_heading(FeatureRequest, :status)
          %th=attr_heading(FeatureRequest, :title)
          %th
        = render @feature_requests

