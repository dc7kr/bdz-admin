.block
  .secondary-navigation
    = render :partial => 'nav'
  .content
    %h2.title
      = t("common.all")
      = t('feature_request',:count=>3)
    .inner
      %table.table.table-hover.table-condensed
        %tr
          %th=attr_heading(Feature_request, :priority')
          %th=attr_heading(Feature_request, :status.title')
          %th=attr_heading(Feature_request, :title')
          %th
        = render @feature_requests

