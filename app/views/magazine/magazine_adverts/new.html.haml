.block
  .secondary-navigation
    =render :partial=>'nav'
  .content
    %h2.title
      = t("common.new")
      = t("magazine_advert")
    .inner
      = form_for @magazine_advert do |f|
        = render :partial => "form", :locals => {:f => f}
