.block
  .secondary-navigation
    =render :partial=>'nav'
  .content
    %h2.title
      = t("common.all")
      = t("event_card.p")
    .inner
      %table.table
        %thead
          %tr
            %th=t('event_card.fest')
            %th=t('event_card.fest_bdz')
            %th=t('event_card.do')
            %th=t('event_card.fr')
            %th=t('event_card.sa')
            %th=t('event_cards.name')
            %th &nbsp;
        = render @event_cards
