.block
  .secondary-navigation
    =render :partial=>'nav'
  .content
    %h2.title
      #{t("event_card")} Nr. #{@event_card.id} 
      - if @event_card.pickup then
        (bereits abgeholt)
    .inner
      %h3=mail_to @event_card.email, @event_card.name

      %table 
        %tr
          %td=label_tag :event_card, :company
          %td=@event_card.company
        %tr
          %td=label_tag :event_card, :street
          %td=@event_card.street
        %tr
          %td=label_tag :event_card, :zip
          %td=@event_card.zip
        %tr
          %td=label_tag :event_card, :city
          %td=@event_card.city
        %tr
          %td=label_tag :event_card, :country_code
          %td=@event_card.country_code
      %h4 #{t('event_card.orderdate')}:  #{format_date @event_card.orderdate}
      %table 
        %tr
          %th=t('common.count')
          %th=t('event_card.card_type')
          %th=t('event_card.single_price')
          %th=t('event_card.total_price')
        - invoice = @event_card.invoice
        - invoice.items.each do |i|
          %tr
            %td=i.count
            %td=i.label
            %td.currency=format_currency(i.price,'€')
            %td.currency=format_currency(i.count*i.price,'€')
        %tr
          %td
          %td #{t("common.sum")}
          %td 
          %td.sum= format_currency(invoice.sum,'€')
      =link_to "PDF", gen_invoice_event_card_path(@event_card)
    
        
