.secondary-navigation
  = render :partial =>'nav'
.container
  .card.bg-light
    .card-body
      %h2.card-title
        = t(".title", id: @event_card.id)
      = render :partial => "form"
- content_for :entity_menu, render(:partial => 'entity_menu')
