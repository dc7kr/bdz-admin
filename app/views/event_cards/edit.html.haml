.secondary-navigation
  = render :partial =>'nav'
.container
  .card.bg-light
    .card-body
      %h2.card-title
        = t("common.edit")
        = t("event_card",:count=>1)
      = render :partial => "form"
- content_for :sidebar, render(:partial => 'sidebar')
