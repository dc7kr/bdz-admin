$('#orchestras').html('#{escape_javascript render(@orchestras)}');
$('#paginator').html('#{escape_javascript(paginate(@orchestras, :remote => true).to_s)}');
$('#entry_info').html('#{escape_javascript(page_entries_info(@orchestras).to_s)}');
