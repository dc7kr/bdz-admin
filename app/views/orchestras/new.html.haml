.secondary-navigation
  = render :partial =>'nav'
.container
  %h2
    = t("common.new")
    = t("orchestra.other")
  = bootstrap_form_for :orchestra, :url => orchestras_path, :html => { :class => "form-horizontal" } do |f|
    = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
