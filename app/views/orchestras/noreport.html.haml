.block
  .secondary-navigation
    =render :partial => 'nav'
  .content
    %h2.title
      = t("orchestra.noreport")
    .inner
      #entry_info
        = page_entries_info @orchestras
      %table.table
        %tr
          %th.first 
            = sortable "mglnr", t("orchestra.mglnr")
          %th= sortable "orchName", t("orchestra.orchName")
          %th= t("orchestra.email")
          %th.last &nbsp;
        =render @orchestras

      = paginate @orchestras
      .actions-bar.wat-cf
        .actions
//- content_for :sidebar, render(:partial => 'sidebar')
