$('#festival_applications').html('#{escape_javascript render(@festival_applications)}');
$('#paginator').html('#{escape_javascript(paginate(@festival_applications, :remote => true).to_s)}');
$('#entry_info').html('#{escape_javascript(page_entries_info(@festival_applications).to_s)}');
