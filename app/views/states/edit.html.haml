.secondary-navigation
  = render :partial=>"nav"
.container
  %h2.title
    = t("web-app-theme.edit", :default => "Edit")
    State
  .inner
    = bootstrap_form_for @state do |f|
      = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
