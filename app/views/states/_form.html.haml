.container-fluid
  = form_wrapped_field(f,@state, :country_code, country_select(:state, :country_code, { iso_codes:true }, { class:  "form-control"}))
  = form_my_field(f, @state, :name)

  .actions
    = submit_button
    = cancel_button() 
