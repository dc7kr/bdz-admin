.block
  .content
    %h2.title
      = t("event_card.order_form.title")
    .inner
      = form_for :event_card, :url => order_success_public_event_cards_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
