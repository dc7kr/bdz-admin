.block
  .secondary-navigation
    =render :partial=>'nav'
  .content
    %h2.title
      #{t("common.list")} #{t("contests.title")}
    .inner
      %table.table.table-hover.table-condensed
        %tr
          %th.first= attr_heading(Course, :startDate)
          %th.first= attr_heading(Course, :endDate)
          %th.first= attr_heading(Course, :title)
          %th.first= attr_heading(Course, :deadline)
          %th.first= attr_heading(Course, :visible)
          
          %th.last &nbsp;
        = render @courses

%br

