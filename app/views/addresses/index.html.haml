.block
  .secondary-navigation
    =render :partial=>'nav'
  .content
    %h2.title
      = t("common.all", :default => "All")
      = t("address.title")
    .inner
      %table.table
        %thead
          %th Anrede
          %th Titel
          %th Vorname
          %th Name
          %th Adresse
          %th Telefon
          %th Mobil
          %th Fax
          %th Email
          %th
      %tbody#addresses
        = render @addresses

%br

= link_to_new(new_address_path,'New')
