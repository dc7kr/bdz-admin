Hallo #{@recipient.name},
\
die automatische Tarifanpassung für Einzelmitglieder wurde durchgeführt.
\
- if @changes.length > 0 
  Folgende Mitglieder wurden umgestellt: 
  - @changes.each do |line|
    #{line}
- else
  Es gab keine Tarifanpassungen
