Hallo #{@recipient.name},

ein neues Rundschreiben wurde von #{@triggered_by} erstellt.
- if @letter_url
  Die verbleibenden Briefe können unter #{@letter_url} heruntergeladen werden.
  \.
- else
  Sämtliche Schreiben wurden per E-Mail versandt.
Ergebnisse:
- if @results.present?
  - @results.each do |res|
    - if not res.nil? then
      |#{res[:mode]}|#{res[:entity].id}|#{res[:entity].email}|
